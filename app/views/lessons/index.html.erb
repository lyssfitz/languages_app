<h1> My Lessons</h1>

<% @lessons.each do |lesson| %>
    <div class="lessons_card">
        <p><%= lesson.lesson_date %></p> 
        <p><%= lesson.language.name %></p> 
        <p><%= lesson.difficulty %></p> 
        <%= link_to "edit", edit_lesson_path(lesson.id)%>
    </div>


<% end %>

<h2> Past Lessons </h2>

<% @lessons.each do |lesson| %>
    <div class="lessons_card">
        <p><%= lesson.lesson_date %></p> 
        <p><%= lesson.language.name %></p> 
        <p><%= lesson.difficulty %></p> 
    </div>


<% end %>
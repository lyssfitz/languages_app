describe("New lessons page", () => {
  before(() => {
    cy.app("clean");
  });

  it("should display the page content", () => {});

  it("should display the page content", () => {});
});
// Amy visits the New Lessons page

// Amy completes the form

// Amy clicks create lesson

// Amy is taken to My Upcoming Lessons page

// Lessons page displays the lesson that Amy created.
